{
  "version": 2,
  "buildCommand": "vite build && pnpm esbuild api/index.ts --bundle --platform=node --format=cjs --outfile=api/_built.js",
  "outputDirectory": "dist/public",
  "framework": null,
  "functions": {
    "api/_built.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/_built.js"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
